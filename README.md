# binary-equivalent-calculator
This program converts numbers from various number systems, including decimal, octal, and hexadecimal, into binary. Users can select the input system and enter a number. The program then outputs the corresponding binary representation.
